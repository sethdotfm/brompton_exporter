# tessera_exporter grafana/provisioning/datasources/prometheus.yaml // Version 1.0
# https://github.com/sethdotfm/tessera_exporter
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
